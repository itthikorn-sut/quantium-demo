name: QA Pipeline

on:
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      run_k6:
        description: 'Run k6 load tests (non-prod only)'
        type: boolean
        default: false
      k6_target_url:
        description: 'k6 target URL (MUST be staging, never production)'
        type: string
        default: 'https://staging.quantiumcore.com'

jobs:
  # ── Job 1: Playwright E2E Tests ───────────────────────────────
  e2e-tests:
    name: E2E Tests (Playwright)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests
        env:
          BASE_URL: https://www.quantiumcore.com
          TEST_EMAIL: ${{ secrets.QA_TEST_EMAIL }}
          TEST_PASSWORD: ${{ secrets.QA_TEST_PASSWORD }}
        run: npx playwright test "tests/e2e/[^/]+\.spec\.ts" --reporter=html,list,json

      - name: Upload Playwright report artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

      - name: Upload E2E results JSON
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-results
          path: test-results.json
          retention-days: 7

  # ── Job 2: Playwright API Tests ───────────────────────────────
  api-tests:
    name: API Tests (Playwright)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run API tests
        env:
          BASE_URL: https://www.quantiumcore.com
          TEST_EMAIL: ${{ secrets.QA_TEST_EMAIL }}
          TEST_PASSWORD: ${{ secrets.QA_TEST_PASSWORD }}
        run: npx playwright test tests/api --reporter=list,json
        # API test failures block merge

      - name: Upload API results JSON
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: api-results
          path: test-results.json
          retention-days: 7

  # ── Job 3: Bug Reproduction Tests ─────────────────────────────
  bug-repro-tests:
    name: Bug Reproduction Tests (known failures)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run bug reproduction tests
        env:
          BASE_URL: https://www.quantiumcore.com
          TEST_EMAIL: ${{ secrets.QA_TEST_EMAIL }}
          TEST_PASSWORD: ${{ secrets.QA_TEST_PASSWORD }}
        run: npx playwright test tests/e2e/bugs --reporter=html,list,json
        continue-on-error: true  # Known bugs — failure expected; logged not blocking

      - name: Upload bug repro report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-bugs
          path: playwright-report/
          retention-days: 7

  # ── Job 4: Publish Playwright Report to GitHub Pages ──────────
  publish-report:
    name: Publish Playwright Report (GitHub Pages)
    runs-on: ubuntu-latest
    needs: [e2e-tests]
    if: always() && (github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch')
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Download Playwright report artifact
        uses: actions/download-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/

      - name: Upload to GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: playwright-report/

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # ── Job 5: AI PR Review ──────────────────────────────────────
  ai-pr-review:
    name: AI QA Review (OpenAI gpt-4o)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      pull-requests: write  # needed to post PR comment
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # full history needed for git diff

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install openai

      - name: Run AI QA review and post PR comment
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_EVENT_NUMBER: ${{ github.event.number }}
          GITHUB_BASE_REF: ${{ github.base_ref }}
        run: python ai-tools/mr_review_bot.py

  # ── Job 6: k6 Load Tests (manual only, NEVER auto on PR) ─────
  k6-load-test:
    name: k6 Load Tests (manual trigger only)
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.run_k6 == 'true'

    steps:
      - uses: actions/checkout@v4

      - name: Install k6
        run: |
          sudo gpg -k
          sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update && sudo apt-get install k6

      - name: Validate target URL is not production
        run: |
          TARGET="${{ github.event.inputs.k6_target_url }}"
          if echo "$TARGET" | grep -q "quantiumcore.com" && ! echo "$TARGET" | grep -q "staging"; then
            echo "ERROR: k6 target URL appears to be production. Aborting."
            exit 1
          fi
          echo "Target URL validated: $TARGET"

      - name: Run k6 dashboard load test
        env:
          LOAD_TEST_BASE_URL: ${{ github.event.inputs.k6_target_url }}
          LOAD_TEST_EMAIL: ${{ secrets.QA_LOAD_TEST_EMAIL }}
          LOAD_TEST_PASSWORD: ${{ secrets.QA_LOAD_TEST_PASSWORD }}
        run: k6 run k6/dashboard-load.js

      - name: Upload k6 results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: k6-results
          path: k6-summary.json
          retention-days: 7
